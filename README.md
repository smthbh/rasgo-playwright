# rasgo-playwright
Requirements:
- nvm / Node.js

Running tests:
  - Install dependencies
      - npm install
  - Install Playwright Browsers
      - npx playwright install --with-deps
  - Run Playwright tests
      - npx playwright test
